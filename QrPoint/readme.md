1. crie um banco de dados com o nome de: db_qrpoint;

2. importe a tabela usuarios no banco de dados;

3. baixe o ambiente execução Nodejs;

4. faça a instalação das bibliotecas do node no terminal utilizando o comando: npm install. a instalação deve ser feito na pasta raiz do projeto;

5. digite na url do navegador: localhost:8081;

6. usuario admin para fazer o login {email: adminMaster@gmail.com, senha: adminM0$};
